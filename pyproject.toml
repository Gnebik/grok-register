[project]
name = "grok-register"
version = "0.1.0"
description = "Grok (x.ai) 自动注册脚本"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4",
    "curl_cffi",
    "python-dotenv",
    "requests",
]

[project.optional-dependencies]
cloak = [
    "cloakbrowser>=0.5.7",
]
